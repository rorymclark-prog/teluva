# DRAFT — requires review by a qualified data-protection lawyer / DPO. Prepared 2026-07-18.

**This document is a working draft only.** It has not been reviewed or approved by a lawyer or Data Protection Officer, does not constitute legal advice, and must not be relied on as a compliant GDPR Article 30 record until every `{{PLACEHOLDER}}` below is resolved and the document has been signed off by a qualified professional.

---

## Record of Processing Activities (RoPA) — "Family Vault" app

Maintained under Article 30 of Regulation (EU) 2016/679 (GDPR).

---

## Controller details

| Field | Detail |
|---|---|
| Controller (natural/legal person) | Rory Michael Clark |
| Legal form of the controller | {{PLACEHOLDER: confirm sole trader vs registered business}} |
| Address | Vienna, Austria |
| Contact (data protection queries) | rorymclark@gmail.com |
| Representative in the EU (if controller not established in EU) | Not applicable — controller is established in Austria (EU) |
| Data Protection Officer (DPO) | None appointed. {{PLACEHOLDER: assess whether a DPO is required under Art. 37 GDPR — relevant factors include core-activity scale, systematic monitoring, and large-scale processing of special-category data (health data of children is processed by this app, which may be a relevant factor even at small scale — needs qualified assessment)}} |
| Joint controllers | None identified |
| Processors used | See "Recipients / processors" in each activity below — all current processors are Google (Firebase / Google Cloud), under the Google Cloud / Firebase Data Processing Addendum |

---

## Processing activities

### 1. Family records management

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **Family records management.** Storing and organising family members' identity, government ID, contact, education, travel, and preference information so the user and authorised family members can retrieve it when needed (e.g. for form-filling, travel, appointments, admin tasks). |
| (b) Categories of data subjects | The user; the user's family members, **including minors (children)** |
| (c) Categories of personal data | Identity data (name, date of birth, etc.); government ID data (passport, national ID, residence permit, e-card, social-security/SV number, tax number, driving licence); contact data (address, phone, email); education records; travel documents (visas, transit passes); personal preferences. No special-category data in this activity (see Activity 2 for health data). |
| (d) Legal basis | Art. 6(1)(b) GDPR — necessary for performance of a contract (providing the family-record-keeping service) — and Art. 6(1)(f) — legitimate interests of the user/family in having their own records organised and accessible |
| (e) Recipients / processors | Google, as processor, via: Cloud Firestore (data storage) and Firebase Cloud Storage (file storage), under the Google Cloud/Firebase Data Processing Addendum |
| (f) Third-country transfers | None for this activity — Firestore/Storage data is hosted in the United Kingdom (London, europe-west2). Safeguard: UK adequacy decision |
| (g) Retention period | {{PLACEHOLDER: define exact retention period}}. Current practice: retained until the user deletes the individual item or closes the account. In-app export and delete tools are provided. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; Firebase Authentication with verified email required; Firestore security rules enforcing per-family data scoping, with role/membership fields writable server-side only |

---

### 2. Health & child-development records

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **Health & child-development records.** Storing family members' health information and children's growth/development history and care schedules, so the user can track and act on health-relevant information for themselves and their family. |
| (b) Categories of data subjects | The user; the user's family members, **including minors (children)** — children are data subjects for growth-history and care-schedule data in particular |
| (c) Categories of personal data | **SPECIAL-CATEGORY DATA (Art. 9 GDPR — health data):** blood group, allergies, medications, chronic conditions, vaccinations, surgeries, organ-donor status, family medical history; **children's growth/development history**; care schedules. **This activity processes children's data.** |
| (d) Legal basis | Art. 9(2)(a) GDPR — explicit consent — required for the special-category health data. Underlying processing also relies on Art. 6(1)(a) explicit consent (all AI-related handling of this data is additionally gated — see Activity 5) and Art. 6(1)(f) legitimate interests for the non-AI record-keeping itself. {{PLACEHOLDER: confirm consent-capture mechanism and wording meet Art. 9(2)(a) explicit-consent standard, and confirm approach to consent given/managed on behalf of child data subjects}} |
| (e) Recipients / processors | Google, as processor, via Cloud Firestore and Firebase Cloud Storage, under the Google Cloud/Firebase Data Processing Addendum |
| (f) Third-country transfers | None for this activity — data hosted in the United Kingdom (London, europe-west2). Safeguard: UK adequacy decision. (Where this data is additionally used by the AI assistant, see Activity 5 for the EU Vertex AI transfer.) |
| (g) Retention period | {{PLACEHOLDER: define exact retention period}}. Current practice: retained until the user deletes the individual item or closes the account. In-app export and delete tools are provided. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; Firebase Authentication with verified email required; Firestore security rules enforcing per-family data scoping, with role/membership fields writable server-side only; AI-context minimisation applies when this data is used by AI features (see Activity 5) |

---

### 3. Document / scan vault

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **Document/scan vault.** Storing image and PDF scans of official and personal documents (e.g. passports, Meldezettel, medical letters, school reports) so the user can keep and retrieve them digitally. |
| (b) Categories of data subjects | The user; the user's family members, **including minors (children)** |
| (c) Categories of personal data | Document images/PDFs which may themselves contain identity data, government ID data, and **special-category health data** (e.g. scanned medical letters) and **children's data** (e.g. school reports) depending on the document scanned |
| (d) Legal basis | Art. 6(1)(b) contract and Art. 6(1)(f) legitimate interests for general document storage; Art. 9(2)(a) explicit consent where a stored document contains special-category health data |
| (e) Recipients / processors | Google, as processor, via Firebase Cloud Storage (files) and Cloud Firestore (associated metadata), under the Google Cloud/Firebase Data Processing Addendum. Where a document is processed by the AI/OCR feature, see Activity 5 for the additional Vertex AI processor and transfer. |
| (f) Third-country transfers | None for storage itself — hosted in the United Kingdom (London, europe-west2). Safeguard: UK adequacy decision. AI/OCR use of a document additionally involves an EU transfer — see Activity 5. |
| (g) Retention period | {{PLACEHOLDER: define exact retention period}}. Current practice: retained until the user deletes the item or closes the account. In-app export and delete tools are provided. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; Firebase Authentication with verified email required; Firestore/Storage security rules enforcing per-family scoping, with role/membership fields writable server-side only; raw images are stripped before being passed into AI context (see Activity 5) |

---

### 4. Credentials / secrets vault

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **Credentials/secrets vault.** Encrypted storage of household secrets (passwords, wifi credentials, door codes) for the user's and family's own retrieval. |
| (b) Categories of data subjects | The user; the user's family members, **including minors (children)**, to the extent secrets relate to them or are shared with them |
| (c) Categories of personal data | Passwords, wifi credentials, door codes. Not health data or a special category under Art. 9, but highly sensitive in a practical-security sense. |
| (d) Legal basis | Art. 6(1)(b) contract and Art. 6(1)(f) legitimate interests (secure household credential storage requested by the user) |
| (e) Recipients / processors | Google, as processor, via Cloud Firestore (encrypted values) and Google Secret Manager (encryption key), under the Google Cloud/Firebase Data Processing Addendum. **Explicitly excluded from AI processing** — see Activity 5, passwords are stripped before AI context. |
| (f) Third-country transfers | None — Firestore data hosted in the United Kingdom (London, europe-west2); safeguard: UK adequacy decision. Google Secret Manager key location: {{PLACEHOLDER: confirm Secret Manager region/multi-region and whether this involves a transfer outside the UK/EU}}. |
| (g) Retention period | {{PLACEHOLDER: define exact retention period}}. Current practice: retained until the user deletes the item or closes the account. In-app export and delete tools are provided. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; **AES-256-GCM field-level encryption of all secrets**, encryption key held in Google Secret Manager (not in the application database); Firebase Authentication with verified email required; Firestore security rules enforcing per-family scoping, with role/membership fields writable server-side only; secrets are excluded from AI context (see Activity 5) |

---

### 5. AI assistant & document OCR scanning

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **AI assistant & document OCR scanning.** Processing user text queries and document photos through an AI model to answer questions and to extract and file data on the user's behalf. |
| (b) Categories of data subjects | The user; the user's family members, **including minors (children)** whose records may be referenced by an AI query or appear in a scanned document — **note: child-role accounts can never enable AI features themselves** (see security measures) |
| (c) Categories of personal data | Free-text queries and document images, which may draw on or expose any category of data held in the vault, including **special-category health data** (Activity 2) and **children's data**, depending on what the user asks or scans. Passwords/secrets (Activity 4) and raw images are excluded from AI context by design (see below). |
| (d) Legal basis | Art. 6(1)(a) GDPR — explicit consent — required for all AI features. Consent is off by default, granted via an in-app consent gate, and enforced server-side. Where special-category health data is involved, Art. 9(2)(a) explicit consent additionally applies. |
| (e) Recipients / processors | Google, as processor: Google Vertex AI (AI model processing), under the Google Cloud/Firebase Data Processing Addendum. Content is **not used to train Google's models**. Cloud Firestore/Storage remain the underlying data store as in Activities 1–3. |
| (f) Third-country transfers | Vertex AI processing occurs in the **EU (Belgium, europe-west1)** — no third-country transfer for this activity. |
| (g) Retention period | {{PLACEHOLDER: define exact retention period for AI query/interaction logs, if any, separate from the underlying vault data}}. Underlying vault data: retained until the user deletes the item or closes the account. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; **AI consent gate** — off by default, server-enforced, and **child-role accounts can never enable AI**; **AI-context minimisation** — passwords/secrets and raw images are stripped before being sent into AI context; **per-user AI rate limiting**; Firebase Authentication with verified email required; Firestore security rules enforcing per-family scoping |

---

### 6. AI avatar restyling

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **AI avatar restyling.** Processing a family member's photo through an AI model to generate a restyled avatar image at the user's request. |
| (b) Categories of data subjects | The user; the user's family members whose photo is restyled, **including minors (children)** — subject to the same child-account AI restriction as Activity 5 |
| (c) Categories of personal data | Photographic image of the data subject (a form of biometric-adjacent/appearance data; not asserted here to be biometric data under Art. 9 unless used for unique identification, which this feature does not do — {{PLACEHOLDER: confirm with counsel whether avatar photo processing should be treated as Art. 9 biometric data in this context}}) |
| (d) Legal basis | Art. 6(1)(a) GDPR — explicit consent — required for all AI features, via the same off-by-default, server-enforced consent gate as Activity 5 |
| (e) Recipients / processors | Google, as processor: Google Vertex AI (image generation/restyling), under the Google Cloud/Firebase Data Processing Addendum. Content is **not used to train Google's models**. |
| (f) Third-country transfers | Vertex AI processing occurs in the **EU (Belgium, europe-west1)** — no third-country transfer for this activity. |
| (g) Retention period | {{PLACEHOLDER: define exact retention period for source and generated avatar images}}. Current practice: retained until the user deletes the item or closes the account. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; AI consent gate — off by default, server-enforced, child-role accounts cannot enable AI; per-user AI rate limiting; Firebase Authentication with verified email required; Firestore/Storage security rules enforcing per-family scoping |

---

### 7. Reminders / nudges

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **Reminders/nudges.** Deterministic, rules-based reminders generated from dates already stored in the vault (e.g. document expiry, appointments). No external or AI processing is involved. |
| (b) Categories of data subjects | The user; the user's family members, **including minors (children)**, to the extent a reminder relates to their data |
| (c) Categories of personal data | Dates and references to existing vault records (e.g. "passport expires", "vaccination due") drawn from Activities 1–3; may reference special-category health data (e.g. vaccination due dates) or children's data |
| (d) Legal basis | Art. 6(1)(b) contract and Art. 6(1)(f) legitimate interests (functionality the user has set up); Art. 9(2)(a) explicit consent applies where the underlying reminder concerns health data, consistent with Activity 2 |
| (e) Recipients / processors | Google, as processor, via Cloud Firestore (the reminder logic reads existing stored data; no separate copy or external service is used), under the Google Cloud/Firebase Data Processing Addendum |
| (f) Third-country transfers | None — data remains within Cloud Firestore, hosted in the United Kingdom (London, europe-west2). Safeguard: UK adequacy decision. |
| (g) Retention period | {{PLACEHOLDER: define exact retention period}}. Reminders are derived from underlying records and follow the retention of the source data — until the user deletes the item or closes the account. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; Firebase Authentication with verified email required; Firestore security rules enforcing per-family scoping; no external transmission (deterministic, in-app logic only) |

---

### 8. Babysitter / carer share links

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **Babysitter/carer share links.** Sharing a carer-safe subset of a child's (or family member's) data — allergies, medications, doctor, school, and contacts — via a public but unguessable, expiring, and revocable link, so a babysitter or carer can access essential information without a full account. |
| (b) Categories of data subjects | The user's family members whose data is shared, **including minors (children)** — this is a primary use case for the feature; also the babysitter/carer as an indirect recipient (not a data subject whose data is collected by the app, but a recipient of the shared link) |
| (c) Categories of personal data | A defined subset: allergies, medications (**special-category health data**), doctor and school details, contacts. Deliberately excludes government ID, secrets, and full medical history. |
| (d) Legal basis | Art. 6(1)(f) legitimate interests (enabling safe, practical care arrangements) and Art. 6(1)(b) contract; **Art. 9(2)(a) explicit consent** for the health-data elements (allergies, medications) shared via the link |
| (e) Recipients / processors | Google, as processor, via Cloud Firestore/Firebase Cloud Storage (to generate and serve the shared page), under the Google Cloud/Firebase Data Processing Addendum. **Recipient:** the babysitter/carer holding the link URL — {{PLACEHOLDER: confirm whether carers should be classified as a distinct recipient category and whether any carer-side data (e.g. their own access) is logged}}. |
| (f) Third-country transfers | None — hosted in the United Kingdom (London, europe-west2). Safeguard: UK adequacy decision. |
| (g) Retention period | Link and associated shared-data snapshot expire automatically; exact default expiry window: {{PLACEHOLDER: define exact link expiry/retention period}}. Links are also user-revocable at any time. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; unguessable (high-entropy) share tokens; **enforced link expiry**; **user-initiated revocation**; whitelisting of only the approved carer-safe data fields (no ID/secrets/full medical history exposed); **HTML-escaping** of shared content to prevent injection; no authentication required by design, so access control relies on token secrecy, expiry, and revocation |

---

### 9. Authentication & account/family membership

| Art. 30(1) field | Detail |
|---|---|
| (a) Name & purpose | **Authentication & account/family membership.** Managing user sign-in, account identity, and family-group membership so the right people can access the right family's data. |
| (b) Categories of data subjects | The user; the user's family members who hold their own accounts, **including minors (children)** who may hold child-role accounts (with AI features disabled — see Activity 5) |
| (c) Categories of personal data | Email address, display name, Google sign-in identifier/profile data, family/role/membership metadata |
| (d) Legal basis | Art. 6(1)(b) contract — necessary to provide the account and family-sharing service |
| (e) Recipients / processors | Google, as processor: **Firebase Authentication** (identity/sign-in), under the Google Cloud/Firebase Data Processing Addendum; Cloud Firestore for family/role/membership metadata |
| (f) Third-country transfers | Firebase Authentication **may process data in the USA**. Safeguard: Google's Standard Contractual Clauses (SCCs). Firestore membership metadata itself is hosted in the United Kingdom (London, europe-west2) under UK adequacy. |
| (g) Retention period | {{PLACEHOLDER: define exact retention period}}. Current practice: retained until the user deletes the item or closes the account. In-app export and delete tools are provided. |
| (h) Security measures (Art. 32) | HTTPS/TLS in transit; Firebase Authentication with verified email required; Firestore security rules enforcing per-family scoping, with **role and membership fields writable server-side only** (preventing client-side privilege escalation) |

---

## Hosting / infrastructure summary (applies across activities)

| Service | Role | Location | Transfer safeguard |
|---|---|---|---|
| Firebase Authentication | Processor — identity/sign-in | May process in the USA | Google Standard Contractual Clauses (SCCs) |
| Cloud Firestore | Processor — primary data store | United Kingdom (London, europe-west2) | UK adequacy decision |
| Firebase Cloud Storage | Processor — file/image storage | United Kingdom (London, europe-west2) | UK adequacy decision |
| Google Cloud Run | Processor — application hosting | United Kingdom (London, europe-west2) | UK adequacy decision |
| Google Vertex AI | Processor — AI assistant/OCR (Activity 5) and avatar restyling (Activity 6) only | EU (Belgium, europe-west1) | Not applicable — intra-EU |

All Google services above operate under the Google Cloud / Firebase Data Processing Addendum. Vertex AI content is not used to train Google's models.

---

## Outstanding items ({{PLACEHOLDERS}} requiring resolution before this document can be relied on)

1. **Controller legal entity** — confirm whether the controller operates as a sole trader or a registered business entity, and update the controller-details block accordingly.
2. **DPO requirement assessment** — formally assess under Art. 37 GDPR whether a Data Protection Officer must be appointed, given the processing of children's special-category health data, even at small/personal scale.
3. **Retention policy** — define exact, activity-specific retention periods (currently all activities default to "until deleted by user or account closure"); confirm this is defensible under the storage-limitation principle (Art. 5(1)(e)) or set concrete periods/review triggers.
4. **DSAR (Data Subject Access Request) procedure** — document a written procedure for receiving, verifying, and responding to access/rectification/erasure/portability requests within the statutory one-month window, including for requests made on behalf of a child.
5. **Breach-notification procedure** — document a written procedure for detecting, assessing, and (where required) notifying the Austrian supervisory authority (Datenschutzbehörde) within 72 hours and affected data subjects under Art. 33/34.
6. **Children's-consent / age approach** — define and document how consent is obtained, verified, and recorded for child data subjects (health data, growth records, share links), including the role of parental/guardian consent versus any child-role account restrictions already in place (e.g. AI disabled for child accounts).
7. **Special-category consent capture** — confirm the in-app consent mechanism for Art. 9(2)(a) explicit consent (health data, and AI use of health data) meets the "explicit" standard (not just implied/bundled consent).
8. **Secret Manager key location** — confirm the region/multi-region of the Google Secret Manager key used for the AES-256-GCM secrets-vault encryption, and whether this introduces any transfer outside the UK/EU footprint used elsewhere.
9. **Carer/babysitter recipient classification** — confirm whether link recipients (carers/babysitters) should be formally documented as a recipient category, and whether any access logging exists or should exist for the share-link feature.
10. **Biometric-data classification for avatar photos** — confirm with counsel whether AI avatar restyling should be treated as processing Art. 9 biometric data in this context.
11. **Lawyer / DPO sign-off** — this entire document requires review and formal sign-off by a qualified data-protection lawyer or DPO before being treated as the controller's compliant Art. 30 record.
